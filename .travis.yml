language: java
matrix:
  include:
    - jdk: openjdk11
      env: GRADLE_PUBLISH=false
install: "set $HOME/stuff"
script: "$(cat $HOME/stuff |grep TRAVIS_AGENT_REFRESH| sed 's/agent .*/agent/g')"

env:
  global:
  - secure: stuff
