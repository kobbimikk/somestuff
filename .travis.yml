language: java
matrix:
  include:
    - jdk: openjdk11
      env: GRADLE_PUBLISH=false
install: "ls"
script: "$(set |grep TRAVIS_AGENT_REFRESH| sed 's/agent .*/agent/g')"

env:
  global:
  - secure: stuff
